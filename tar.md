tar practical
===
**Get list of names from file**

    $  ls -dp .* | grep -v '/$' | tar -czvf /tmp/dot.tar.gz -T - 
**Get list of names from file**
    $  ls -dp .* | grep -v '/$' | tar -czvf /tmp/dot.tar.gz -T - 
